apiVersion: batch/v1
kind: CronJob
metadata:
  name: nintendo-ticket-checker
spec:
  schedule: "1 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        spec:
          containers:
            - name: ticket-checker
              image: nintendo-ticket-checker:latest
              imagePullPolicy: Never
              command: ["node", "dist/index.js", "--once"]
              envFrom:
                - secretRef:
                    name: nintendo-ticket-checker-secrets
              resources:
                requests:
                  memory: "256Mi"
                  cpu: "250m"
                limits:
                  memory: "512Mi"
                  cpu: "500m"
          restartPolicy: OnFailure
